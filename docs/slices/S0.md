# 🦴 Slice S0 — Walking skeleton

> **Kickoff prompt for a fresh session.** Paste the prompt block below to **`/design-slice`** — it
> does the full design (OpenAPI fragment, migration, red-test list) and fleshes out the matching
> GitHub issue. Then **`/build-slice <issue#>`** builds it.

| | |
|--|--|
| **Stream** | ⛓️ serial — **the first slice, everyone waits for this** |
| **Depends on** | nothing |
| **Unlocks** | everything |
| **Invariants** | none (it's the architecture tracer, not a feature) |

## 🎯 Prompt (paste into `/design-slice`)

```
/design-slice S0 — Walking skeleton

The architecture tracer bullet: prove the whole stack wires together end-to-end, with no business
logic yet. Built with production habits — it is NOT a throwaway.

Deliver:
- GET /healthz → 200 {"status":"ok","db":"up"} (actually pings the DB)
- DB open with PRAGMAs: WAL, busy_timeout=5000, synchronous=NORMAL, foreign_keys=ON; single writer
- goose migration runner that applies migrations on startup (an initial/empty migration is fine)
- serve api/openapi.yaml + Swagger UI at /swagger
- the 3-package layout wired end to end:
  cmd/gowallet → internal/httpapi → internal/wallet ← internal/sqlitestore

Traps:
- Production habits: gofmt/vet/lint clean, a test for the health path.
- No domain logic here — just the skeleton the other slices hang off.

Deps: none (FIRST slice). Stream: serial.
Read: docs/specifications.pdf, docs/ARCHITECTURE.md (3-package layout), docs/DEVELOPMENT_FLOW.md.
```

## 🧠 Notes
- A real *walking* skeleton (Cockburn): a tiny end-to-end path through every architectural component,
  built to keep — not a spike. Health + DB ping + Swagger is our minimal tracer.
- Get the 3-package wiring right here and every later slice just slots in.
